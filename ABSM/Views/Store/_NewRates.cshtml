@model IEnumerable<ABSM.Models.RateList>

<!-- New Arrivals
================================================== -->
<div class="container">
    <!-- Headline -->
    <div class="sixteen columns">
        <h3 class="headline">New Rates</h3>
        <span class="line margin-bottom-0"></span>
    </div>
    <!-- Carousel -->
    <div id="new-arrivals" class="showbiz-container sixteen columns">
        <!-- Navigation -->
        <div class="showbiz-navigation">
            <div id="showbiz_left_1" class="sb-navigation-left"><i class="fa fa-angle-left"></i></div>
            <div id="showbiz_right_1" class="sb-navigation-right"><i class="fa fa-angle-right"></i></div>
        </div>
        <div class="clearfix"></div>
        <!-- Products -->
        <div class="showbiz" data-left="#showbiz_left_1" data-right="#showbiz_right_1" data-play="#showbiz_play_1">
            <div class="overflowholder">
                <ul>
                    @foreach (var rate in Model)
                    {

                        <li>
                            <figure class="product">
                                <div class="mediaholder">
                                    <a href="@Url.Action("Details","Store", new { id = rate.ProductID })">
                                        <img alt="" src="@Url.Content(rate.Product.ImageUrl)" />
                                        <div class="cover">
                                            <img alt="" src="@Url.Content(rate.Product.ImageUrl)" />
                                        </div>
                                    </a>
                                </div>
                                <a href="variable-product-page.html">
                                    <section>
                                        <a href="@Url.Action("Browse","Store", new { id = rate.Product.ProductID })">
                                            <span class="product-category">@rate.Product.Name</span>
                                        </a>
                                        <h5>@rate.Product.Name</h5>
                                        <span class="product-price">PKR @rate.Price | @rate.UpdatedDate.ToString("dd MMM yyyy")</span>
                                    </section>
                                </a>
                            </figure>
                        </li>
                    }
                    <!-- Product #1 -->



                </ul>
                <div class="clearfix"></div>
            </div>
            <div class="clearfix"></div>
        </div>
    </div>
</div>

